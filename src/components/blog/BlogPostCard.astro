---
import { Icon } from "astro-icon/components";

import type { CollectionEntry } from "astro:content";

interface Props {
    blogPost: CollectionEntry<"blogs">;
}

const { blogPost } = Astro.props as Props;

const { title, tags, description, author, pubDate } = blogPost.data;

const url = `/blog/${blogPost.slug}/`;

const dotw = pubDate.toLocaleDateString("default", { weekday: "short" }); // Day of The Week
const date = `${pubDate.getDate() < 10 ? "0" : ""}${pubDate.getDate().toString().slice(-2)}`;
const month = pubDate.toLocaleString("default", { month: "short" });
const year = pubDate.getFullYear();
---

<div class="flex flex-col space-y-2 md:space-y-4 dark:bg-[#33333340] bg-[#DDDDDD23] dark:border-DM-Title-Bar border-LM-Title-Bar border-2 fill-none rounded-xl px-2 py-2 md:dark:bg-[#33333340] md:bg-[#DDDDDD23] md:dark:border-DM-Title-Bar md:border-LM-Title-Bar md:border-4 md:rounded-2xl md:px-4 md:py-2">
    <div class="flex flex-col space-y-2 sm:space-y-0 md:flex-row md:space-x-8 mb-1 items-start md:items-center">
        <h3 class="font-title text-[20px] md:text-[25px] font-medium text-LM-Title dark:text-DM-Title tracking-[-.05rem]">
            <a href={url} >{title}</a>
        </h3>
        <ul class="grid grid-cols-auto grid-rows-2 grid-flow-col sm:ml-0 sm:flex sm:flex-row sm:space-x-0.5 font-sans text-sm text-LM-Para dark:text-DM-Para">
            {tags.map((tag, idx) => (
                    <> <li> <a class="hover:text-LM-Nav-Unselected dark:hover:text-DM-Nav-Selected mr-4" href="#" > {tag} </a> </li>
                        {idx < tags.length - 1 && ( <Icon name="dot" size={20} class="hidden-mobile text-LM-Nav-Unselected dark:text-DM-Nav-Selected"/> )}
                    </>  )) }
        </ul>
    </div>
    <p class="font-para text-base text-LM-Para w-full lg:w-9/12 dark:text-DM-Para"> {description.toString()} </p>
    <div class="mt-1 flex flex-row items-center">
        <a class="font-title text-[14px] tracking-[-0.3px] font-normal text-LM-Nav-Unselected dark:text-DM-Nav-Selected hover:text-LM-Nav-Selected dark:hover:text-DM-Nav-Unselected" href="#"> {author} </a>
        <Icon name="dot" size={20} class="text-LM-Nav-Unselected dark:text-DM-Nav-Selected"/>
        <span
            class="font-sans uppercase text-[13px] tracking-[-0.02rem] font-medium text-LM-Title dark:text-DM-Title"
            >{`${dotw}, ${date} ${month} ${year}`}</span
        >
    </div>
</div>

<style>
    @media (max-width: 640px) {
    .hidden-mobile {
      display: none;
    }
  }
</style>