---
import katex from 'katex';

const { formula = '' } = Astro.props;
const html = katex.renderToString(formula, {throwOnError: false});
---

<div class="text-center text-[11px] md:text-[16px] my-10">
	<span set:html={html} class="" />
</div>
